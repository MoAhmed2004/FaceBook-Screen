import 'package:flutter/material.dart';
import 'package:second_assignment/home_Screen.dart';

class Myapp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomeScreen(),
    );
  }
}